

<div class="container mt-5 student-form">
    <div class="row justify-content-center">
        <div class="col-lg-5">
            <a href="/student" class="btn-sm btn btn-warning shadow-sm ">Step Back</a>
            <div class="card shadow mt-4">
                <div class="card-header">
                    <h2 class="text-center">Upadate <%= students.name %>  Data</h2>
                </div>
                <div class="card-body">
                    <form action="/student/update/<%= students.id %>" method="POST">
                        <div class="my-3">
                            <label for="">Name</label>
                            <input name="name" value="<%= students.name %>" type="text" class="form-control">
                        </div>
                        <div class="my-3">
                            <label for="">Email</label>
                            <input type="text" value="<%= students.email %>" name="email" class="form-control">
                        </div>
                        <div class="my-3">
                            <label for="">Cell</label>
                            <input type="text" value="<%= students.cell %>" name="cell" class="form-control">
                        </div>
                        <div class="my-3">
                            <label for="">Location</label>
                            <select name="location" id="" class="form-control">
                                <option value="">-select-</option>
                                                                <option <% if (students.location == "Dhaka") { %> selected <% } %>value="Dhaka">Dhaka</option>
                                <option <% if (students.location == "Khulna") { %> selected <% } %>value="Khulna">Khulna</option>
                                <option <% if (students.location == "Slyhet") { %> selected <% } %> value="Slyhet">Slyhet</option>
                                <option <% if (students.location == "Rajshahi") { %> selected <% } %>value="Rajshahi">Rajshahi</option>
                                <option <% if (students.location == "Rongpur") { %> selected <% } %> value="Rongpur">Rongpur</option>
                                <option <% if (students.location == "Barisal") { %> selected <% } %>value="Barisal">Barisal</option>
                            </select>
                        </div>
                        <div class="my-3">
                            <label for="">Photo</label> 
                            <br>
                            <% if (students.photo == 'avatar.png') { %>
                                <img class="preload-student" src="/images/<%= students.photo %>" alt="">
                                <% } else { %>
                                    <img class="preload-student" src="/images/students/<%= students.photo %>" alt="">                              
                            <% } %>
                            <br>
                            <label for="upload_student_photo"><img src="https://static.thenounproject.com/png/212328-200.png" alt=""></label>
                            <input name="student-photo" type="file" id="upload_student_photo">
                        </div>
                        <div class="my-3">
                            <input type="submit" class="btn btn-warning w-100" value="Update Now">
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
